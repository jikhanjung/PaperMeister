.env
__pycache__/
*.pyc
*.db
Trilobite Shape/
.vscode/settings.json
.claude/settings.local.json

# PyInstaller build artifacts (PaperMeister.spec is tracked; output is not)
/build/
/dist/
/.build-venv/

# Inno Setup — generated installer output (template is tracked)
/installer/Output/
/installer/PaperMeister.iss

# Linux AppImage build (generated)
/build_linux/
/AppDir/

# macOS build (generated)
/PaperMeister-macOS-*.dmg
/dist/PaperMeister.app/

# Sphinx manual build output
docs/manual/_build/

# Editor swap files
*.swp
*.swo
*~

# 공통 PaleoBytes 가이드 체크아웃(비공개 devdocs)을 가리키는 심볼릭 링크.
# 복사하지 않고 참조만 한다 — 사본은 원본과 어긋난다. devlog 087 참조.
.guides
