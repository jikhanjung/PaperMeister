Changelog
=========

Release notes are maintained in ``CHANGELOG.md`` at the repository root, which
is also what the GitHub release pages quote. It is included here rather than
copied, so the two can never drift apart.

.. mdinclude is not used; myst_parser handles the include below.

.. include:: ../../CHANGELOG.md
   :parser: myst_parser.sphinx_
